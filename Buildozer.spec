[app]
title = FlashlightApp
package.name = flashlightapp
package.domain = org.yourname
source.include_exts = py,png,jpg,kv
version = 0.1
requirements = python3,kivy,plyer
android.permissions = CAMERA, FLASHLIGHT

# (Other buildozer.spec settings as needed)
